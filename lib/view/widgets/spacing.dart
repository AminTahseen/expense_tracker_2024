import 'package:flutter/material.dart';

class AddSpacing extends StatelessWidget {
  const AddSpacing({super.key});

  @override
  Widget build(BuildContext context) {
    return const Padding(padding: EdgeInsets.all(10.0));
  }
}
